# 209_长度最小的子数组

---- Lee

## 一、题目

给定一个含有 n 个正整数的数组和一个正整数 s ，找出该数组中满足其和 ≥ s 的长度最小的 连续 子数组，并返回其长度。如果不存在符合条件的子数组，返回 0。

示例：

- 输入：s = 7, nums = [2,3,1,2,4,3]
- 输出：2
- 解释：子数组 [4,3] 是该条件下的长度最小的子数组。

提示：

- 1 <= target <= 10^9
- 1 <= nums.length <= 10^5
- 1 <= nums[i] <= 10^5

[力扣题目链接(opens new window)](https://leetcode.cn/problems/minimum-size-subarray-sum/)

## 二、思路

* 暴力法，两个`for`循环嵌套，进行计算和比较

* 滑动窗口：

  * 使用双指针组成一个窗口。
  * 当窗口内的元素满足条件后，计算当前长度与历史计算结果对比取最小值。
  * 移动起始位置的窗口，继续判单是否满足条件，若能继续移动窗口，直至不满足条件，移动终止位置的窗口。

* 滑动窗口三问：

  * 窗口内是什么？

    * 数组中的元素，逐个累加观察是否满足条件。

  * 窗口的起始位置如何移动？

    * 满足条件后，计算并比较获取最小长度，移动起始位置。

    ```c
            while (sum >= target)
            {
                currentLength = j - i + 1;
                result = result < currentLength ? result : currentLength;
                // 窗口的起始位置如何移动？当sum>target是，可以移动
                sum -= nums[i++];
            }
    ```

  * 窗口的终止位置如何移动?

    * 当前窗口内元素不满足条件时，继续移动。

* 长度如何获取？

  * 第一直觉`length=0`是错误的：与后续计算的长度取最小。就会发现最后所有的结果都是0。很尴尬😅
  * 设置一个当前变量的最大值`INT_MAX`，这样就可以获取到计算的长度了。
  * `INT_MAX`也有风险：若窗口中元素累加都不满足条件，则返回结果就是`INT_MAX`。在返回时，要在进行一个判断：

  ```c
  // 返回结果
  return result == INT_MAX ? 0 : result;
  ```

  ## 三、代码

  ```c
  int minSubArrayLen(int target, int* nums, int numsSize) {
      // 入参校验
      if (NULL == nums || 0 >= numsSize)
      {
          return 0;
      }
  
      // 创建双变量，组成滑动窗口
      int i = 0;
      int j = 0;
  
      // 创建存储结果的变量
      int result = INT_MAX;
      int sum = 0;
      int currentLength = 0;
  
      // 窗口内是什么？：是连续的数组元素
      for (j = 0; j < numsSize; ++j)
      {
          // 窗口的终止位置如何移动？遍历数组中元素时移动
          sum += nums[j];
          while (sum >= target)
          {
              currentLength = j - i + 1;
              result = result < currentLength ? result : currentLength;
              // 窗口的起始位置如何移动？当sum>target是，可以移动
              sum -= nums[i++];
          }
      }
      // 返回结果
      return result == INT_MAX ? 0 : result;
  }
  ```

  