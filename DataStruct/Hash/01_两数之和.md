# 01_两数之和

***---- Li Jinsheng***

***

链接：[Two Sum](https://leetcode.cn/problems/two-sum/)

### 一、思维

#### 1.1 目的：

找出所给数组中和满足要求的两个数的下标

#### 1.2思考：

1.2.1 暴力解法

1.2.2 Hash解法：

* map底层实现为红黑树，既key值不能重复
* 可以使用Hash实现值与其下标的映射关系通过“traget - arr[i]”完成判断。

```c++
class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        vector<int> result(2, -1);
        unordered_map<int, int> map;
        for (int i = 0; i < nums.size(); ++i)
        {
            auto iter = map.find(target - nums[i]);
            if (iter != map.end())
            {
                return {iter->second, i};
            }
            map.insert(pair<int, int>{nums[i], i});
        }

        return result;
    }
};
```

